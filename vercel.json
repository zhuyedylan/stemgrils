{
  "buildCommand": "apt-get update && apt-get install -y pandoc && npm run build",
  "outputDirectory": "build",
  "installCommand": "npm install",
  "framework": "docusaurus-2",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}